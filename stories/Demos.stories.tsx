import * as React from "react";
import { Example } from "../src";

// eslint-disable-next-line import/no-default-export
export default {
  title: "Demos/flow",
};

export const Basic = () => {
  return <Example />;
};
